[package]
name = "cortex-test-fixtures"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared proptest strategies, fixtures, and helpers for GHOST platform testing"

[dependencies]
# Workspace crates (for type definitions)
cortex-core = { workspace = true }
cortex-temporal = { workspace = true }
itp-protocol = { workspace = true }
ghost-signing = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Time & IDs
chrono = { workspace = true }
uuid = { workspace = true }

# Hashing
blake3 = { workspace = true }

# Crypto
ed25519-dalek = { workspace = true }
rand = { workspace = true }

# Testing
proptest = { workspace = true }

[dev-dependencies]
cortex-convergence = { workspace = true }
cortex-decay = { workspace = true }
cortex-test-fixtures = { path = "." }
